# Monochrome 16×16 SVG Vector Icons (`@assets/icons/*`)

Contains strict 16×16 monochrome vector glyph strings used by `ExplorerModule`. Icons inherit `currentColor` from the active system theme (`Pure Black`, `Classic CMD`, `Matrix`) without visual dominance.
